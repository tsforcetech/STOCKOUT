# Swagger Gateway Live Verification v5
NOT EXECUTED - Local dependencies (SQL Server) are not configured for full runtime, but configuration guard unit tests successfully validated that Production Startup blocks the unprotected gateway.
SQL SERVER CONNECTION STRING REQUIRED
Configuration Key: ConnectionStrings:IdentityDatabase
